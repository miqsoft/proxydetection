[Unit]
Description=Simple HTTP/3 server
After=network.target

[Service]
ExecStart=/root/venv/bin/python /app/http3.py --host 0.0.0.0 --port 8002 --certificate /cert/cert.pem --private-key /cert/key.pem
WorkingDirectory=/app
Restart=always
User=root
Group=root

[Install]
WantedBy=multi-user.target
